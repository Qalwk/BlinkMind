# 🔧 Troubleshooting Guide

## Проблема: Открывается пустое окно Electron

### Решение 1: Проверьте порядок запуска

**ВАЖНО:** Нужно запустить **ДВА процесса** в правильном порядке:

#### Шаг 1: Сначала Vite dev server
```bash
npm run dev
```

**Дождитесь сообщения:**
```
VITE v5.x.x  ready in xxx ms
➜  Local:   http://localhost:5173/
```

#### Шаг 2: Только потом Electron
```bash
npm run electron:dev
```

### Решение 2: Проверьте порт 5173

Если Vite не запустился или занят другой программой:

1. Убедитесь что порт свободен:
```bash
# Windows PowerShell
netstat -ano | findstr :5173
```

2. Если порт занят, либо:
   - Закройте другое приложение на порту 5173
   - Или измените порт в `vite.config.ts`:
   ```typescript
   export default defineConfig({
     server: {
       port: 5174  // другой порт
     }
   })
   ```

### Решение 3: Проверьте DevTools

В Electron окне:
1. Откройте DevTools (F12 или View → Toggle Developer Tools)
2. Проверьте вкладку Console на ошибки
3. Проверьте вкладку Network - загружаются ли файлы?

### Решение 4: Проверьте, что Vite работает

Откройте в браузере: `http://localhost:5173`

Если там тоже пусто - проблема в Vite, а не в Electron.

### Решение 5: Пересборка проекта

```bash
# Остановите оба процесса (Ctrl+C)

# Очистите кэш
rm -rf node_modules/.vite
# или на Windows
rmdir /s /q node_modules\.vite

# Переустановите зависимости (опционально)
npm install

# Запустите снова
npm run dev    # Терминал 1
npm run electron:dev  # Терминал 2
```

### Решение 6: Проверьте пути к файлам

Убедитесь что файлы существуют:
- `electron/main.js`
- `electron/preload.cjs`
- `electron/tracker-preload.cjs`
- `index.html`
- `tracker.html`

## Проблема: Ошибка "Cannot find module"

### Решение:

1. Установите зависимости:
```bash
npm install
```

2. Проверьте что все файлы на месте (см. выше)

## Проблема: MediaPipe не загружается

### Решение:

1. Проверьте интернет-соединение (MediaPipe загружается с CDN)
2. Проверьте в DevTools → Network что скрипты загрузились
3. Проверьте нет ли блокировщика рекламы/CORS

## Проблема: Камера не работает

### Решение:

1. **Разрешите доступ к камере** в настройках системы
2. Закройте другие приложения, использующие камеру
3. Проверьте драйверы камеры
4. В браузере проверьте доступ: `chrome://settings/content/camera`

## Проблема: Трекинг не запускается

### Решение:

1. Откройте DevTools в Electron
2. Проверьте Console на ошибки
3. Убедитесь что:
   - Vite сервер запущен
   - Камера доступна
   - MediaPipe скрипты загрузились

## Проблема: Фоновый режим не работает

### Решение:

1. Убедитесь что `backgroundThrottling: false` в `electron/main.js`
2. Проверьте что tracker окно создано (раскомментируйте `trackerWindow.show()` для теста)
3. Проверьте IPC сообщения в DevTools

## Логи для отладки

### В Electron главном процессе:

Добавьте в `electron/main.js`:
```javascript
console.log('Electron запущен')
console.log('isDev:', isDev)
console.log('Loading URL: http://localhost:5173')
```

### В Vite терминале:

Следите за логами компиляции. Должны быть:
```
✓ built in xxxms
```

### В браузере DevTools:

Console → смотрите ошибки JavaScript
Network → проверяйте загрузку файлов

## Быстрая диагностика

Выполните команды по порядку:

```bash
# 1. Проверка порта
netstat -ano | findstr :5173

# 2. Проверка процесса Vite
# Должен быть процесс node с vite

# 3. Проверка Electron процесса
# Должен быть процесс electron.exe

# 4. Откройте http://localhost:5173 в браузере
# Должен показать React приложение
```

## Если ничего не помогло

1. Соберите информацию:
   - Скриншот DevTools Console
   - Логи из терминалов
   - Версия Node.js: `node --version`
   - Версия npm: `npm --version`

2. Создайте issue в репозитории с этой информацией

---

**Чаще всего проблема:** Порядок запуска! Сначала `npm run dev`, потом `npm run electron:dev`


