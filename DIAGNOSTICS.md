# 🔍 Диагностика проблемы "Камера не активна"

## Симптомы

- ✅ Статус показывает "Активен"
- ✅ Кнопка "Остановить трекинг" доступна (isTracking = true)
- ❌ Показывает "Камера не активна"
- ❌ Длительность 00:00:00
- ❌ Нет данных трекинга (latestData = null)

## Причины

### 1. Tracker окно не получает команду start-tracking

**Проверка:**
1. Откройте tracker окно (должно быть видно)
2. Откройте Console tracker окна (F12)
3. Нажмите "Начать трекинг" в главном окне
4. Проверьте есть ли лог: `[Tracker] Команда начать трекинг`

**Если нет лога:**
- IPC связь не работает
- Проверьте что tracker окно создано
- Проверьте Console главного окна на ошибки

### 2. Камера не запускается в tracker окне

**Проверка:**
В Console tracker окна ищите:
- `[Tracker] Инициализация FaceTracker...`
- `[Tracker] FaceTracker инициализирован успешно`
- `[Tracker] ✅ Камера успешно запущена` (после start())

**Если нет:**
- Может быть ошибка запуска камеры
- Проверьте системные настройки доступа к камере
- Проверьте что камера не занята другим приложением

### 3. Статус камеры не отправляется в главное окно

**Проверка:**
В Console главного окна ищите:
- `[IPC] Статус камеры обновлен: {active: true, ...}`

**Если нет:**
- IPC канал `camera-status` не работает
- Проверьте electron/main.js → setupIpcHandlers()

### 4. Данные трекинга не приходят

**Проверка:**
В Console главного окна ищите:
- `[IPC] Получены данные трекинга: {...}`

**Если нет:**
- MediaPipe не обрабатывает кадры
- Лицо не обнаружено
- IPC канал `tracker-data` не работает

## Пошаговая диагностика

### Шаг 1: Проверьте tracker окно

```javascript
// В tracker окне Console выполните:
console.log('Tracker окно работает:', !!window.trackerAPI)
console.log('MediaPipe загружен:', !!window.FaceMesh)
```

### Шаг 2: Проверьте IPC связь

```javascript
// В главном окне Console:
console.log('Electron API доступен:', !!window.electronAPI)

// Проверьте что команды отправляются
window.electronAPI.startTracking({...}) // должно отправиться в tracker окно
```

### Шаг 3: Проверьте камеру напрямую

```javascript
// В tracker окне Console:
navigator.mediaDevices.getUserMedia({ video: true })
  .then(stream => {
    console.log('✅ Камера доступна!', stream)
    stream.getTracks().forEach(t => t.stop())
  })
  .catch(err => console.error('❌ Ошибка камеры:', err))
```

### Шаг 4: Проверьте MediaPipe

```javascript
// В tracker окне Console:
console.log('FaceMesh:', typeof window.FaceMesh)
console.log('Camera:', typeof window.Camera)
```

## Быстрое решение

Если видите "Камера не активна" при активном трекинге:

1. **Откройте tracker окно** (должно быть видно для отладки)
2. **Проверьте Console tracker окна** на ошибки
3. **Перезапустите трекинг:**
   - Нажмите "Остановить трекинг"
   - Подождите 2 секунды
   - Нажмите "Начать трекинг" снова

4. **Если не помогает:**
   - Закройте все приложения с камерой
   - Перезапустите Electron полностью
   - Проверьте настройки приватности системы

## Логи для проверки

После нажатия "Начать трекинг" должны появиться логи:

**В главном окне:**
```
[Smart Sequence] Запрос доступа к камере и запуск трекинга...
[Smart Sequence] ✅ Трекинг запущен, ожидание данных от tracker окна...
```

**В tracker окне:**
```
[Tracker] Команда начать трекинг {...}
[Tracker] ✅ Камера успешно запущена
```

**Снова в главном окне:**
```
[IPC] Статус камеры обновлен: {active: true, initialized: true}
[IPC] Получены данные трекинга: {faceDetected: false, ...}
```

Если какого-то лога нет - проблема в этом месте!

