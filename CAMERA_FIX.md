# 📹 Исправление проблемы с камерой

## Диагностика

### Шаг 1: Проверьте tracker окно

После запуска должно открыться **ДВА окна**:
1. Главное окно (FocusTracker UI)
2. **Tracker окно** (должно быть видно для отладки)

Если tracker окно не видно, проверьте `electron/main.js` - там должно быть:
```javascript
trackerWindow.show() // ВРЕМЕННО для отладки камеры
```

### Шаг 2: Проверьте Console в tracker окне

В DevTools tracker окна проверьте:

1. **Загрузился ли MediaPipe?**
   - Должно быть: `✅ MediaPipe готов к использованию`
   - Или ошибка: `❌ MediaPipe FaceMesh не загружен!`

2. **Инициализируется ли трекер?**
   - Ищите: `[Tracker] Начало инициализации...`
   - Или ошибки инициализации

3. **Есть ли ошибки камеры?**
   - Проверьте на ошибки типа: `NotAllowedError`, `NotFoundError`, `NotReadableError`

### Шаг 3: Типичные проблемы

#### Проблема: "MediaPipe FaceMesh не загружен"

**Причины:**
- Нет интернет-соединения
- Блокировщик рекламы блокирует CDN
- CORS ошибки

**Решение:**
1. Проверьте интернет
2. Отключите блокировщики (uBlock, AdBlock)
3. Проверьте Console → Network → посмотрите на загрузку скриптов

#### Проблема: "NotAllowedError" - доступ к камере запрещен

**Причины:**
- Не разрешили доступ к камере
- Системные настройки блокируют камеру
- Другое приложение использует камеру

**Решение:**

**Windows:**
1. Настройки → Конфиденциальность → Камера
2. Разрешите доступ к камере для приложений
3. Перезапустите приложение

**macOS:**
1. Системные настройки → Безопасность и конфиденциальность → Камера
2. Разрешите доступ для Electron
3. Перезапустите приложение

**Linux:**
- Убедитесь что есть доступ к `/dev/video0` или `/dev/video1`

#### Проблема: "NotFoundError" - камера не найдена

**Причины:**
- Камера не подключена
- Неправильный драйвер
- Камера используется другим приложением

**Решение:**
1. Проверьте подключение камеры
2. Закройте другие приложения (Zoom, Skype, etc.)
3. Обновите драйверы камеры

#### Проблема: "NotReadableError" - камера занята

**Причины:**
- Другое приложение использует камеру
- Системная служба использует камеру

**Решение:**
1. Закройте все приложения с камерой
2. Перезагрузите компьютер
3. Проверьте в Диспетчере задач, нет ли процессов, использующих камеру

### Шаг 4: Ручная проверка камеры в браузере

1. Откройте `http://localhost:5173` в обычном браузере (Chrome)
2. Разрешите доступ к камере
3. Проверьте работает ли камера там

Если в браузере не работает - проблема в системе/камере, не в приложении.

### Шаг 5: Проверка через DevTools

В tracker окне выполните в Console:

```javascript
// Проверка MediaPipe
console.log('FaceMesh:', typeof window.FaceMesh)
console.log('Camera:', typeof window.Camera)

// Проверка доступа к камере
navigator.mediaDevices.getUserMedia({ video: true })
  .then(stream => {
    console.log('✅ Камера доступна!')
    console.log('Stream:', stream)
    stream.getTracks().forEach(track => track.stop())
  })
  .catch(error => {
    console.error('❌ Ошибка доступа к камере:', error)
  })
```

## Что делать дальше

### Если MediaPipe не загружается:

1. Проверьте интернет
2. Попробуйте другой CDN или локальную установку MediaPipe
3. Проверьте Console → Network на ошибки загрузки

### Если камера не доступна:

1. Проверьте системные настройки приватности
2. Закройте другие приложения
3. Перезагрузите приложение

### Если все выше не помогло:

1. Соберите логи:
   - Console из tracker окна
   - Console из главного окна
   - Ошибки из терминалов

2. Проверьте версии:
   ```bash
   node --version
   npm --version
   ```

3. Создайте issue с описанием проблемы и логами

## Быстрая проверка

После перезапуска приложения должно быть:

✅ **Два окна открыты** (главное + tracker)  
✅ **В tracker Console:** `✅ MediaPipe готов к использованию`  
✅ **После нажатия "Начать трекинг":** Камера запрашивается и начинает работать  
✅ **Статус меняется:** "Инициализация..." → "Готов к запуску" → "Трекинг активен"  

Если что-то из этого не выполняется - смотрите соответствующий раздел выше.


