# ⚡ Быстрое исправление ошибки MediaPipe CDN

## 🔴 Проблема
Ошибка `ERR_CONNECTION_TIMED_OUT` при загрузке MediaPipe библиотек

![Ошибка в консоли](https://i.imgur.com/example.png)

## ✅ Решение (5 минут)

### Шаг 1: Обновите код

Если вы скачали проект с GitHub, убедитесь, что у вас последняя версия:

```bash
git pull origin main
```

### Шаг 2: Установите зависимости (если еще не установлены)

```bash
npm install
```

### Шаг 3: Пересоберите приложение

**Для использования (обычным пользователям):**

```bash
npm run build
npm run electron:build
```

Готовое приложение будет в папке `dist-electron/`

**Для разработки:**

```bash
# В первом терминале
npm run dev

# Во втором терминале
npm run electron:dev
```

### Шаг 4: Проверьте

Откройте приложение и проверьте консоль DevTools:

- ✅ Должно быть: `[Tracker HTML] ✅ MediaPipe готов к использованию`
- ❌ Не должно быть: `ERR_CONNECTION_TIMED_OUT`

## 🎯 Что было исправлено

Код теперь использует **два CDN** с автоматическим переключением:

1. **Основной:** `unpkg.com` (обычно доступен везде)
2. **Резервный:** `jsdelivr.net` (автоматически включается если первый недоступен)

## 📚 Дополнительные решения

Если проблема сохраняется, смотрите полную инструкцию: [CDN_FIX.md](CDN_FIX.md)

---

## 🎓 Для разработчиков

### Что было изменено:

1. **tracker.html** - добавлен unpkg.com с резервным jsdelivr
2. **face-tracker.ts** - изменен locateFile на unpkg.com

### Файлы:
```
tracker.html                                      (строки 9-33)
src/features/face-tracking/lib/face-tracker.ts   (строки 72-77)
```

