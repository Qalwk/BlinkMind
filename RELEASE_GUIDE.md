# 🚀 Руководство по созданию релиза BlinkMind

Это пошаговая инструкция, как собрать и опубликовать релиз приложения на GitHub.

---

## 📋 Предварительные требования

### 1. Установите зависимости
```bash
npm install
```

### 2. Убедитесь, что код работает локально
```bash
npm run dev          # Проверка веб-версии
npm run electron:dev # Проверка Electron-версии
```

---

## 🔧 Подготовка к релизу

### Шаг 1: Обновите версию

Откройте `package.json` и измените версию:
```json
{
  "version": "1.0.0"  // Было "0.1.0"
}
```

**Форматы версий:**
- `1.0.0` - первый стабильный релиз
- `1.0.1` - патч (исправление багов)
- `1.1.0` - минорное обновление (новые фичи)
- `2.0.0` - мажорное обновление (ломающие изменения)

### Шаг 2: Создайте иконку приложения (опционально)

Для профессионального вида создайте иконку:
- **Windows:** `build/icon.ico` (256x256 или больше)
- **macOS:** `build/icon.icns` (512x512 или больше)
- **Linux:** `build/icon.png` (512x512)

Если нет иконки - используется стандартная Electron.

---

## 🏗️ Сборка релиза

### Вариант А: Сборка для вашей ОС

Соберите приложение для вашей операционной системы:

```bash
npm run build
npm run electron:build
```

**Результаты сборки:**
- Файлы появятся в папке `dist-electron/`
- **Windows:** `BlinkMind-Setup-X.X.X.exe`
- **macOS:** `BlinkMind-X.X.X.dmg`
- **Linux:** `BlinkMind-X.X.X.AppImage`

### Вариант Б: Кросс-платформенная сборка

Чтобы собрать для всех ОС (требуется macOS для сборки .dmg):

**На Windows:**
```bash
npm run build
npm run electron:build -- --win --linux
```

**На macOS:**
```bash
npm run build
npm run electron:build -- --mac --win --linux
```

**На Linux:**
```bash
npm run build
npm run electron:build -- --linux --win
```

> ⚠️ **Примечание:** Полная кросс-платформенная сборка работает лучше всего на macOS из-за ограничений code signing.

---

## 📤 Загрузка на GitHub

### Шаг 1: Закоммитьте изменения

```bash
git add .
git commit -m "Release v1.0.0"
git push origin main
```

### Шаг 2: Создайте тег версии

```bash
git tag v1.0.0
git push origin v1.0.0
```

### Шаг 3: Создайте релиз на GitHub

#### Через веб-интерфейс GitHub:

1. Откройте ваш репозиторий на GitHub
2. Перейдите в раздел **"Releases"** (справа на главной странице)
3. Нажмите **"Draft a new release"** (создать новый релиз)
4. Заполните форму:
   - **Tag version:** `v1.0.0` (выберите созданный тег)
   - **Release title:** `BlinkMind v1.0.0 - Первый релиз 🎉`
   - **Description:** Напишите changelog (что нового)
5. Загрузите собранные файлы:
   - Перетащите файлы из `dist-electron/` в раздел "Attach binaries"
   - Загрузите `.exe`, `.dmg`, `.AppImage` и др.
6. Нажмите **"Publish release"**

#### Через командную строку (GitHub CLI):

Если установлен `gh` CLI:

```bash
# Создайте релиз
gh release create v1.0.0 \
  --title "BlinkMind v1.0.0 - Первый релиз" \
  --notes "Первая стабильная версия с помодоро и трекингом!"

# Загрузите файлы
gh release upload v1.0.0 dist-electron/BlinkMind-Setup-*.exe
gh release upload v1.0.0 dist-electron/BlinkMind-*.dmg
gh release upload v1.0.0 dist-electron/BlinkMind-*.AppImage
```

---

## 📝 Пример Changelog

Вот пример описания для релиза:

```markdown
## 🎉 BlinkMind v1.0.0

Первая стабильная версия приложения для отслеживания концентрации!

### ✨ Основные возможности

- 👁️ **Трекинг концентрации** через веб-камеру
  - Анализ моргания, позы головы, направления взгляда
  - Расчет уровня вовлеченности в реальном времени
  
- 🍅 **Помодоро-таймер**
  - Настраиваемые интервалы работы и отдыха
  - Автоматическое сохранение сессий
  - Статистика и стрики
  
- 📊 **Детальная статистика**
  - Календарь с историей сессий
  - Графики вовлеченности
  - Отслеживание прогресса

- 🔐 **Приватность**
  - Все данные хранятся локально
  - Никаких отправок в интернет

### 📥 Установка

**Windows:**
Скачайте `BlinkMind-Setup-1.0.0.exe` и запустите установщик

**macOS:**
Скачайте `BlinkMind-1.0.0.dmg`, откройте и перетащите в Applications

**Linux:**
Скачайте `BlinkMind-1.0.0.AppImage`, сделайте исполняемым и запустите

### 📖 Документация

См. [USER_GUIDE.md](USER_GUIDE.md) для подробной инструкции

### 🐛 Известные проблемы

- Нет

---

**Полный Changelog:** https://github.com/ваш-username/BlinkMind/commits/v1.0.0
```

---

## 🔄 Автоматизация (опционально)

### GitHub Actions для автоматической сборки

Создайте файл `.github/workflows/release.yml`:

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build app
        run: npm run electron:build

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: ${{ matrix.os }}-build
          path: dist-electron/*
```

**Как использовать:**
1. Создайте этот файл в репозитории
2. Запушьте тег: `git push origin v1.0.0`
3. GitHub автоматически соберет приложение для всех ОС
4. Скачайте артефакты и добавьте в релиз

---

## 📊 Checklist перед релизом

Перед публикацией проверьте:

- [ ] Версия обновлена в `package.json`
- [ ] Все функции работают корректно
- [ ] Нет критических багов
- [ ] README.md актуален
- [ ] USER_GUIDE.md актуален
- [ ] Создан changelog
- [ ] Приложение собирается без ошибок
- [ ] Протестирована установка на чистой системе
- [ ] Создан git tag
- [ ] Файлы загружены на GitHub
- [ ] Релиз опубликован

---

## 🎯 Быстрый запуск (TL;DR)

```bash
# 1. Обновите версию в package.json
# 2. Соберите приложение
npm run build
npm run electron:build

# 3. Создайте тег и запушьте
git add .
git commit -m "Release v1.0.0"
git push origin main
git tag v1.0.0
git push origin v1.0.0

# 4. Создайте релиз на GitHub и загрузите файлы из dist-electron/
```

---

## 🆘 Проблемы при сборке

### Ошибка: "Cannot find module"
```bash
rm -rf node_modules package-lock.json
npm install
```

### Ошибка: "electron-builder error"
```bash
npm install electron-builder --save-dev
```

### Ошибка на macOS: "App is damaged"
Это из-за отсутствия code signing. Для разработки:
```bash
xattr -cr /Applications/BlinkMind.app
```

Для продакшена нужен Apple Developer Account ($99/год).

### Ошибка на Windows: "Cannot create symbolic link"

**Проблема:** При сборке ошибка "Cannot create symbolic link" с winCodeSign

**Решение:** Запустите PowerShell от имени администратора:

1. Нажмите **Win + X** → **"Windows PowerShell (администратор)"**
2. Перейдите в папку проекта: `cd D:\Projects\BlinkMind`
3. Запустите сборку: `npm run electron:build`

Или быстрая команда из обычного PowerShell:
```powershell
Start-Process powershell -Verb RunAs -ArgumentList "-NoExit", "-Command", "cd '$PWD'; npm run electron:build"
```

### Ошибка на Windows: Antivirus блокирует
Добавьте папку проекта в исключения антивируса.

---

## 📞 Нужна помощь?

- **Документация electron-builder:** https://www.electron.build/
- **GitHub Releases:** https://docs.github.com/en/repositories/releasing-projects-on-github

---

Готово! Теперь ваше приложение доступно для скачивания! 🎉

