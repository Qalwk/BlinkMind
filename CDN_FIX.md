# 🔧 Исправление проблемы с загрузкой MediaPipe

## 🐛 Проблема

У вас появилась ошибка `ERR_CONNECTION_TIMED_OUT` при загрузке библиотек MediaPipe с CDN `jsdelivr.net`:

```
GET https://cdn.jsdelivr.net/npm/@mediapipe/face_mesh/face_mesh.js net::ERR_CONNECTION_TIMED_OUT
```

Это происходит когда CDN `jsdelivr.net` недоступен или заблокирован в вашем регионе.

## ✅ Решение 1: Альтернативный CDN (РЕКОМЕНДУЕТСЯ)

Я уже внес изменения в код, которые используют альтернативный CDN `unpkg.com` с автоматическим переключением на `jsdelivr.net` при необходимости.

### Что нужно сделать:

1. **Пересоберите приложение:**

```bash
npm run build
npm run electron:build
```

2. **Или запустите в режиме разработки:**

```bash
# В первом терминале
npm run dev

# Во втором терминале
npm run electron:dev
```

3. **Готово!** Теперь приложение будет пытаться загрузить библиотеки сначала с `unpkg.com`, а если не получится - с `jsdelivr.net`.

## 🔄 Решение 2: Локальные файлы (если CDN не работают)

Если оба CDN недоступны, можно скачать библиотеки локально:

### Шаг 1: Создайте папку для библиотек

```bash
mkdir -p public/mediapipe
```

### Шаг 2: Скачайте файлы вручную

Скачайте следующие файлы и положите в папку `public/mediapipe/`:

1. **face_mesh.js**:
   - https://unpkg.com/@mediapipe/face_mesh@0.4.1633559619/face_mesh.js

2. **camera_utils.js**:
   - https://unpkg.com/@mediapipe/camera_utils@0.3.1632432234/camera_utils.js

3. **drawing_utils.js**:
   - https://unpkg.com/@mediapipe/drawing_utils@0.3.1620248257/drawing_utils.js

4. **Файлы модели Face Mesh** (скачайте всю папку):
   - https://unpkg.com/@mediapipe/face_mesh@0.4.1633559619/

### Шаг 3: Измените tracker.html

Откройте `tracker.html` и замените строки 10-12 на:

```html
<script src="/mediapipe/face_mesh.js"></script>
<script src="/mediapipe/camera_utils.js"></script>
<script src="/mediapipe/drawing_utils.js"></script>
```

### Шаг 4: Измените face-tracker.ts

Откройте `src/features/face-tracking/lib/face-tracker.ts` и найдите строку 74-76, замените на:

```typescript
this.faceMesh = new FaceMesh({
  locateFile: (file: string) => {
    return `/mediapipe/${file}`
  }
})
```

### Шаг 5: Пересоберите приложение

```bash
npm run build
npm run electron:build
```

## 🌐 Решение 3: Использовать VPN

Если проблема в блокировке CDN в вашем регионе:

1. Включите VPN
2. Запустите приложение
3. После загрузки MediaPipe можно отключить VPN - данные кэшируются браузером

## 🔍 Проверка решения

После применения любого решения:

1. Откройте DevTools (Ctrl+Shift+I / Cmd+Option+I)
2. Перейдите на вкладку Console
3. Вы должны увидеть:
   ```
   [Tracker HTML] ✅ MediaPipe готов к использованию
   [Tracker HTML] window.FaceMesh: ✓ Загружен
   [Tracker HTML] window.Camera: ✓ Загружен
   ```

Если вместо этого видите:
```
[Tracker HTML] ❌ MediaPipe FaceMesh не загружен!
```

Значит нужно использовать Решение 2 (локальные файлы).

## 📝 Примечания

- **Решение 1** (альтернативный CDN) - самое простое, уже применено
- **Решение 2** (локальные файлы) - требует ручного скачивания, но работает без интернета
- **Решение 3** (VPN) - временное решение

## 🆘 Если ничего не помогло

1. Проверьте интернет-соединение
2. Проверьте файрвол/антивирус - они могут блокировать CDN
3. Попробуйте отключить прокси
4. Создайте Issue на GitHub с описанием проблемы

---

**Для разработчиков:** Все изменения внесены в:
- `tracker.html` - альтернативный CDN с резервным вариантом
- `src/features/face-tracking/lib/face-tracker.ts` - использование unpkg.com

