# 🎥 Исправление логики работы с камерой

## Проблемы которые были найдены

### 1. Race Condition: команды терялись
**Проблема:** Когда пользователь нажимал "Разрешить доступ к камере" сразу после запуска приложения, команда `start-tracking` отправлялась в tracker окно которое еще не загрузилось. Команда терялась.

**Решение:** 
- Добавлена очередь команд (`pendingCommands`) в `electron/main.js`
- Tracker окно отправляет сигнал `tracker-ready` после инициализации
- Накопленные команды обрабатываются после получения сигнала готовности

### 2. IPC слушатели регистрировались слишком поздно
**Проблема:** В `src/tracker.tsx` слушатели IPC команд (`onStartTracking`, `onStopTracking`) регистрировались ВНУТРИ async функции `initializeTracker()`. Если команда приходила до завершения инициализации - она пропадала.

**Решение:**
- Разделены IPC слушатели и инициализация трекера на 2 отдельных `useEffect`
- IPC слушатели регистрируются СРАЗУ при монтировании компонента
- Инициализация трекера происходит параллельно

### 3. Неправильное управление состоянием сессии
**Проблема:** `startSession()` вызывалась СРАЗУ в `requestCameraAccess()`, устанавливая `isTracking=true` ДО того как камера реально запустилась. Это создавало несоответствие: UI показывал "Активен", но камера была неактивна.

**Решение:**
- Удалено преждевременное `startSession()` из `requestCameraAccess()`
- `startSession()` теперь вызывается в `onCameraStatus` callback когда `status.active === true`
- Сессия запускается ТОЛЬКО когда камера реально работает

### 4. Отсутствовала обратная связь о готовности
**Проблема:** Main процесс не знал когда tracker окно готово принимать команды.

**Решение:**
- Добавлен метод `sendReady()` в `TrackerAPI`
- Tracker отправляет сигнал `tracker-ready` после успешной инициализации
- Main процесс устанавливает флаг `trackerReady = true`

## Новая логика работы (правильная последовательность)

### Инициализация при запуске

```
1. Electron запускается → создает main окно и tracker окно
2. Tracker окно загружается (loadURL)
3. Tracker.tsx монтируется
4. Регистрируются IPC слушатели (useEffect #1)
5. Начинается инициализация FaceTracker (useEffect #2)
6. MediaPipe загружается
7. FaceTracker создается и инициализируется
8. Tracker отправляет сигнал "tracker-ready"
9. Main процесс устанавливает trackerReady = true
10. UI готов принимать команды
```

### Запуск камеры (пользователь нажимает кнопку)

```
1. Пользователь нажимает "Разрешить доступ к камере"
2. handleRequestCamera() вызывает requestCameraAccess()
3. requestCameraAccess() отправляет api.startTracking(settings)
4. Main процесс получает команду "start-tracking"
5. Main проверяет trackerReady:
   - Если false → добавляет в pendingCommands
   - Если true → отправляет в tracker окно
6. Tracker окно получает команду через IPC слушатель
7. Tracker вызывает tracker.start()
8. FaceTracker запрашивает доступ к камере через getUserMedia
9. Пользователь разрешает (или уже разрешено)
10. Camera.start() завершается успешно
11. Tracker отправляет "camera-status" {active: true}
12. Main перенаправляет статус в main окно
13. useElectronIntegration получает статус через onCameraStatus
14. Проверка: если status.active && !currentSession → startSession()
15. Сессия запускается, isTracking становится true
16. UI обновляется: показывает "Активен"
17. FaceTracker начинает отправлять данные трекинга
18. UI получает данные и отображает визуализацию
```

## Ключевые изменения в коде

### electron/main.js

```javascript
// ДОБАВЛЕНО: Очередь команд
let trackerReady = false
let pendingCommands = []

// ДОБАВЛЕНО: Обработчик готовности
ipcMain.on('tracker-ready', () => {
  trackerReady = true
  // Обработка накопленных команд
  while (pendingCommands.length > 0) {
    const cmd = pendingCommands.shift()
    trackerWindow.webContents.send(cmd.channel, cmd.data)
  }
})

// ИЗМЕНЕНО: Проверка готовности перед отправкой
ipcMain.on('start-tracking', (event, settings) => {
  if (!trackerReady) {
    pendingCommands.push({ channel: 'start-tracking', data: settings })
    return
  }
  trackerWindow.webContents.send('start-tracking', settings)
})
```

### src/tracker.tsx

```javascript
// НОВОЕ: Отдельный useEffect для IPC слушателей
useEffect(() => {
  const api = getTrackerAPI()
  
  // Регистрируем слушатели СРАЗУ
  api.onStartTracking((settings) => { /* ... */ })
  api.onStopTracking(() => { /* ... */ })
  api.onSettingsUpdate((settings) => { /* ... */ })
}, [])

// Отдельный useEffect для инициализации
useEffect(() => {
  async function initializeTracker() {
    // ... инициализация ...
    await tracker.initialize(videoElement, canvasElement)
    
    // КРИТИЧНО: Отправляем сигнал готовности
    api.sendReady()
  }
  
  initializeTracker()
}, [])
```

### src/app/providers/useElectronIntegration.ts

```javascript
// ИЗМЕНЕНО: НЕ вызываем startSession() здесь
const requestCameraAccess = async () => {
  // Только отправляем команду, НЕ меняем состояние
  api.startTracking(currentSettings)
  // startSession() вызовется когда камера станет активна
}

// ИЗМЕНЕНО: startSession() вызывается здесь
api.onCameraStatus((status) => {
  updateCameraStatus(status)
  
  // Сессия запускается ТОЛЬКО когда камера активна
  if (status.active && !useTrackingStore.getState().currentSession) {
    useTrackingStore.getState().startSession()
  }
})
```

## Логи для диагностики

После исправлений вы должны видеть следующую последовательность логов:

### При запуске приложения:

```
[Tracker] Регистрация IPC слушателей...
[Tracker] IPC слушатели зарегистрированы
[Tracker] Начало инициализации...
[Tracker] Инициализация FaceTracker...
[FaceTracker] Camera instance создан
[Tracker] FaceTracker инициализирован успешно
[Tracker] ✅ Готов к работе, сигнал отправлен в main процесс
[Main] ✅ Tracker окно готово к работе
```

### При нажатии "Разрешить доступ к камере":

```
[Camera Request] Отправка команды запуска трекинга в tracker окно...
[Main] Получена команда start-tracking, tracker ready: true
[Main] Отправка команды start-tracking в tracker окно
[Tracker] Команда начать трекинг
[FaceTracker] Запуск камеры...
[FaceTracker] ✅ Камера запущена успешно
[FaceTracker] Статус камеры отправлен: active=true
[Tracker] ✅ Камера успешно запущена
[Main] Получен статус камеры от tracker: {active: true, initialized: true}
[IPC] Статус камеры обновлен: {active: true, initialized: true}
[IPC] ✅ Камера активна! Запускаем сессию трекинга
[IPC] Получены данные трекинга: {faceDetected: false, ...}
```

## Проверка исправлений

1. Перезапустите приложение полностью
2. Откройте DevTools для обоих окон (главного и tracker)
3. Нажмите "Разрешить доступ к камере"
4. Проверьте логи - должны появиться ВСЕ логи из последовательности выше
5. Статус должен смениться на "🟢 Активен" только когда камера реально запустится
6. Должна начаться визуализация данных трекинга

## Типичные ошибки

### Если камера не запускается:

1. **MediaPipe не загружен** - проверьте что CDN доступен
2. **Доступ запрещен** - проверьте настройки приватности системы
3. **Камера занята** - закройте другие приложения с камерой
4. **Tracker не готов** - дождитесь лога "✅ Tracker окно готово к работе"

### Если статус "Инициализация..." не меняется:

1. Проверьте что tracker окно загрузилось
2. Проверьте что `tracker-ready` сигнал был отправлен
3. Проверьте Console на ошибки инициализации MediaPipe

## Итог

Теперь логика работы правильная:
- ✅ Команды не теряются (очередь)
- ✅ IPC слушатели готовы до отправки команд
- ✅ Сессия запускается только при активной камере
- ✅ Состояния синхронизированы
- ✅ Подробные логи для диагностики

